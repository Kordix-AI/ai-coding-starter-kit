{
  "name": "kordix-content-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Kordix Content Factory — automatisierte faceless YouTube-Content-Maschine (multi-niche, multi-channel). Audio ist die zeitliche Source of Truth.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "slice": "tsx src/pipeline/run-slice.ts"
  },
  "dependencies": {
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.2"
  }
}
